spring:
  datasource:
    url: ${app.db.url}
    username: ${app.db.user}
    password: ${app.db.pass}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

logging:
  level:
    root: INFO
