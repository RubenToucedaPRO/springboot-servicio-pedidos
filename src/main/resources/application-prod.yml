spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pedidos
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

# Optional: connection pool settings (HikariCP is default in Spring Boot)
# spring:
#   datasource:
#     hikari:
#       maximum-pool-size: 10
#       minimum-idle: 2

# Logging and other production-friendly settings can go here
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: health,info
